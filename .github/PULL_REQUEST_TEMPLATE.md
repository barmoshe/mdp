## What changed



## Checklist

- [ ] Every `examples/*.mdp` and `templates/*.mdp` builds and is byte-identical across two runs; `npm run check:examples` passes.
- [ ] Output is deterministic: built twice, the HTML is identical.
- [ ] The design lock holds: no gradients, no shadows, one ink ramp, two weights.
- [ ] Spec and docs updated if the format changed.
